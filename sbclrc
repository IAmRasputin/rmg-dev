;;; The following lines added by ql:add-to-init-file:
#-quicklisp
(let ((quicklisp-init (merge-pathnames "quicklisp/setup.lisp"
                                       (user-homedir-pathname))))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))

#+quicklisp
(push #p"~/code/lisp/" ql:*local-project-directories*)

(defun :cd (path)
  (uiop:chdir path)
  (setf *default-pathname-defaults* (truename path)))

(when (and (interactive-stream-p *terminal-io*) (not (member "--no-linedit" (uiop:command-line-arguments) :test #'equalp)))
  (ql:quickload '(linedit alexandria serapeum)  :silent t)
  (defpackage #:rmg
    (:use :cl :cl-user :alexandria :serapeum))
  (in-package #:rmg)
  (funcall (intern "INSTALL-REPL" :linedit) :wrap-current t :eof-quits t))


